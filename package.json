{
  "name": "bolt-ts-search-template",
  "version": "1.0.0",
  "description": "A scaffold template for Slack apps",
  "main": "dist/app.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node ./dist/app.js",
    "lint": "npx @biomejs/biome check *.ts listeners tests",
    "lint:fix": "npx @biomejs/biome check --write *.ts listeners tests",
    "test": "npm run build && npm run lint && node --import tsx --test"
  },
  "author": "Slack Technologies, LLC",
  "license": "MIT",
  "keywords": [
    "slack",
    "bolt",
    "slackapi",
    "typescript"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/slack-samples/bolt-ts-search-template.git"
  },
  "bugs": {
    "url": "https://github.com/slack-samples/bolt-ts-search-template/issues"
  },
  "dependencies": {
    "@slack/bolt": "^4.6.0",
    "dotenv": "~17.2.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@slack/cli-hooks": "^1.2.1",
    "typescript": "^5.9.2",
    "tsx": "^4.20.6"
  }
}
